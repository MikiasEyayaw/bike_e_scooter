import 'package:flutter/material.dart';

class MaintenanceHistoryList extends StatelessWidget {
  const MaintenanceHistoryList({super.key});

  @override
  Widget build(BuildContext context) => const Text('Maintenance History List');
}