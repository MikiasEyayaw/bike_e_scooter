import 'package:flutter/material.dart';
class MaintenanceStatusChip extends StatelessWidget {
  const MaintenanceStatusChip({super.key});
  @override
  Widget build(BuildContext context) => const Text('Maintenance Status Chip');
}