import 'package:flutter/material.dart';

class MaintenanceFilterBar extends StatelessWidget {
  const MaintenanceFilterBar({super.key});
  @override
  Widget build(BuildContext context) => const Text('Maintenance Filter Bar');
}