import 'package:flutter/material.dart';

class TripFilterBar extends StatelessWidget {
  const TripFilterBar({super.key});
  @override
  Widget build(BuildContext context) => const Text('Trip Filter Bar');
}