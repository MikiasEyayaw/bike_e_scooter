import 'package:flutter/material.dart';
class TripMapView extends StatelessWidget {
  const TripMapView({super.key});
  @override
  Widget build(BuildContext context) => const Text('Trip Map View');
}