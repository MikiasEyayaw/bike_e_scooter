import 'package:flutter/material.dart';
class TripDurationDisplay extends StatelessWidget {
  const TripDurationDisplay({super.key});

  @override
  Widget build(BuildContext context) => const Text('Trip Duration Display');
}