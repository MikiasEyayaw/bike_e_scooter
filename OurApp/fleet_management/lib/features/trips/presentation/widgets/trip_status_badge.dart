import 'package:flutter/material.dart';
class TripDetailInfo extends StatelessWidget {
  const TripDetailInfo({super.key});
  @override
  Widget build(BuildContext context) => const Text('Trip Detail Info');
}
