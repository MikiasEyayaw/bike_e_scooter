import 'package:flutter/material.dart';
class VehicleStatusBadge extends StatelessWidget {
  const VehicleStatusBadge({super.key});
  @override
  Widget build(BuildContext context) => const Text('Vehicle Status Badge');
}