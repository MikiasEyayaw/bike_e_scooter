import 'package:flutter/material.dart';

class VehicleSearchBar extends StatelessWidget {
  const VehicleSearchBar({super.key});
  @override
  Widget build(BuildContext context) => const Text('Vehicle Search Bar')
  ;
}