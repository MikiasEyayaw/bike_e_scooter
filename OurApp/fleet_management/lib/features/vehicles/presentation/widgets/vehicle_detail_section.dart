import 'package:flutter/material.dart';

class VehicleDetailSection extends StatelessWidget {
  const VehicleDetailSection({super.key});
  @override
  Widget build(BuildContext context) => const Text('Vehicle Detail Section');
}