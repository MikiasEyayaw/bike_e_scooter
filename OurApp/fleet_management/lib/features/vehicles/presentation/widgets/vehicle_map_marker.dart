import 'package:flutter/material.dart';
class VehicleMapMarker extends StatelessWidget {
  const VehicleMapMarker({super.key});
  @override
  Widget build(BuildContext context) => const Text('Vehicle Map Marker');
}