import 'package:flutter/material.dart';

class VehicleItemTile extends StatelessWidget {
  const VehicleItemTile({super.key});
  @override
  Widget build(BuildContext context) => const Text('Vehicle Item Tile');
}









